pipeline {
    agent any

    stages {
        stage('Build') {
			environment {
			HOME = "."
			}
            steps {       	
                echo 'Building..'
				script {
                    sh(returnStdout: true, script: 'echo true.sh')
                }
            }
		}
        
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}

